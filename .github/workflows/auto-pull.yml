name: Auto Pull on Server
on:
  push:
    branches: [ main ]

jobs:
  pull:
    runs-on: ubuntu-latest
    steps:
      - name: SSH and pull
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.168.107.60.11 }}
          username: ${{ secrets.ubuntu }}
          key: ${{ secrets.ORACLE_SSH_KEY }}
          script: |
            cd /home/ubuntu/Hi-Dunkey
            git pull
            echo "✅ Pull 완료: $(date)"